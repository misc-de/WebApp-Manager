# Runtime dependencies for WebApp Manager.
#
# PyGObject additionally needs the system GTK 4 / Libadwaita libraries and
# their GObject-Introspection data, which pip cannot install:
#   Debian/Ubuntu: python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
#   Arch:          python-gobject gtk4 libadwaita
#   Fedora:        python3-gobject gtk4 libadwaita
#
# On most distributions installing the packages above is preferable to
# `pip install -r requirements.txt`, because PyGObject then matches the
# system GTK build exactly.

PyGObject>=3.42
Pillow>=9.0
